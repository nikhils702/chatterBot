class Recon:
    conversation = [
        "Recon",
        "Reconciliation report",
        "What is reconciliation?",
        "Reconciliation is used to do a quick reconcile between the Statement and FIRE output and OFX output.",
        "Usage for reconciliation",
        "perl 1099Reconciliation.pl --taxYear=<taxYear> --clientId=<clientId> --formId=<formId> --reportUI=Y --forceRun=N --ofxRecon=Y",
        "how to execute reconciliation program",
     "perl 1099Reconciliation.pl --taxYear=<taxYear> --clientId=<clientId> --formId=<formId> --reportUI=Y --forceRun=N --ofxRecon=Y",   
    ]
